{{>header}}
<div id="car-listings">
    <h1>Car Listings</h1>

    <div id="listings">

        {{#each cars}}
            {{>listCars}}
        {{else}}
        <p class="no-cars">No cars in database.</p>
        {{/each}}

    </div>
</div>
{{>footer}}